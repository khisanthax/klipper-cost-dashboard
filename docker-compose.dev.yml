version: "3.8"

services:
  kcd:
    build: .
    container_name: kcd
    restart: unless-stopped
    ports:
      - "6060:5000"
    volumes:
      - ./data:/app/data
    environment:
      - TZ=America/New_York
